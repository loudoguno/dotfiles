# Install Homebrew
/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
# Tell OS_to prioritize programs installed by Homebrew (in /usr/local/bin) rather than OS Default
$ echo 'export PATH="/usr/local/bin:$PATH"' >> ~/.bash_profile


# run brewfile



# Check that homebrew is installed correctly and self diagnose
brew doctor
# Update the index of available packages (aka recipes) and cask
brew update

# View list of installed recipes
brew list
brew list --versions
# See what needs to be updated
brew outdated

# install a package
brew install AppName
	-mas
# Update an application
brew upgrade AppNam
brew info AppName
# Remove outdated versions from the cellar.
brew cleanup AppName
#Uninstall formula
brew remove AppName



# Taps - Add formula hosted on non-homebrew repos (taps) (http://docs.brew.sh/brew-tap.html)
# list currently traccked taps
brew tap



# Cask
# Allows you to install GUI Apps
# comes installed with Homebrew directly

brew tap caskroom/cask
brew install caskroom/cask/brew-cask

# Install via Cask
install
# Check casks installed
ls /usr/local/Caskroom
search
uninstall


-cask install alfred
cask install --appdir="~/Applications" google-chrome


# Bundle
# Process brew file in current directory, adding brew to beginning of each line
brew bundle
# create brewfile form all existing Homebrew Packages installed
brew bundle dump






END