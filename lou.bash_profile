# Lou.bash_profile
# This file lives on GitHub and is to be sourced/coooopied into the users home folder.

export PATH="/usr/local/bin:$PATH"
[ -f $HOME/Dropbox/Sync/Bash/bash_profile ] && source $HOME/Dropbox/Sync/Bash/bash_profile